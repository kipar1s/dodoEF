# Система управления заказами пиццерии (dodoEF)

> **Курсовая работа** по дисциплине «Программирование» / «Базы данных»  
> **Автор:** Козионов Денис Евгеньевич, группа ЭУ-310
> **Учебный год:** 2025/2026

---

## 📋 Описание проекта

Настольное приложение для управления заказами пиццерии с использованием Entity Framework Core и базы данных Microsoft Access.

Система позволяет:
- Создавать, редактировать и удалять заказы.
- Выбирать клиента из справочника.
- Добавлять в заказ несколько товаров с указанием количества.
- Отслеживать состав товаров из ингредиентов.
- Назначать ответственного сотрудника и способ оплаты.
- Хранить данные о клиентах, товарах, ингредиентах, сотрудниках и квалификациях.

---

## 🛠 Технологический стек

| Компонент | Технология |
|:---|:---|
| **Язык программирования** | C# |
| **Платформа** | .NET 10.0 |
| **Графический интерфейс** | Windows Forms (WinForms) |
| **Доступ к данным** | Entity Framework Core 10+ |
| **Провайдер БД** | EntityFrameworkCore.Jet (OLE DB) |
| **База данных** | Microsoft Access (.accdb) |
| **Миграции** | EF Core Migrations |

---

## 📁 Структура проекта

```
dodoEF/
├── MyEF/Entities/                 # Классы сущностей и DbContext
│   ├── ApplicationDbContext.cs    # Контекст базы данных
│   ├── Client.cs                  # Клиент
│   ├── Oder.cs                    # Заказ
│   ├── Tovar.cs                   # Товар
│   ├── Ingr.cs                    # Ингредиент
│   ├── Plateg.cs                  # Способ оплаты
│   ├── Personal.cs                # Сотрудник
│   ├── Categori.cs                # Категория сотрудника
│   ├── Evelobilitile.cs           # Уровень квалификации
│   ├── OderTovar.cs               # Связь заказ-товар
│   ├── TovarIngr.cs               # Связь товар-ингредиент
│   └── PersonalEvelobilitile.cs   # Связь сотрудник-квалификация
├── OderForm/                      # Windows Forms для работы с заказами
│   ├── OderListForm.cs            # Список заказов
│   ├── OderFormUnit.cs            # Детали заказа (создание/редактирование)
│   └── OderSelectionDialog.cs     # Диалог выбора клиента
├── Migrations/                    # Миграции EF Core (автогенерируемые)
├── Program.cs                     # Точка входа
├── dodoDb2.accdb                  # Файл базы данных Access
├── dodoEF.csproj
└── README.md
```

---

## ⚙️ Установка и запуск

### Требования

- Windows 10/11
- Microsoft .NET 10.0 SDK (или Visual Studio 2022 с поддержкой .NET 10.0)
- Microsoft Access Database Engine (для работы с .accdb)
- Visual Studio 2022 (рекомендуется)

### Сборка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/kipar1s/dodoEF.git
   ```

2. Откройте `dodoEF.sln` в Visual Studio.

3. Восстановите пакеты NuGet:
   ```bash
   dotnet restore
   ```

4. При необходимости отредактируйте строку подключения в `ApplicationDbContext.cs`:
   ```csharp
   optionsBuilder.UseJet(@"Provider=Microsoft.ACE.OLEDB.12.0;Data Source=путь_к_dodoDb2.accdb");
   ```

5. Примените миграции (создаст БД, если её нет):
   ```bash
   dotnet ef database update
   ```

6. Нажмите **F5** для запуска.

---

## 🗄 База данных

База данных `dodoDb2.accdb` (Microsoft Access) создаётся автоматически при первом запуске через `Database.EnsureCreated()`.

### Основные сущности и связи

| Сущность | Назначение | Ключевые связи |
|:---|:---|:---|
| **Client** | Клиенты | один ко многим с `Oder` |
| **Oder** | Заказы | один к одному с `Plateg`, многие ко многим с `Tovar` (через `OderTovar`) |
| **Tovar** | Товары | многие ко многим с `Ingr` (через `TovarIngr`) |
| **Ingr** | Ингредиенты | многие ко многим с `Tovar` |
| **Plateg** | Способы оплаты | один к одному с `Oder` |
| **Personal** | Сотрудники | один ко многим с `Oder`, многие ко многим с `Evelobilitile` |
| **Categori** | Категории сотрудников | один ко многим с `Personal` |
| **Evelobilitile** | Уровни квалификации | многие ко многим с `Personal` |

Связи многие-ко-многим реализованы через промежуточные таблицы (`OderTovar`, `TovarIngr`, `PersonalEvelobilitile`).

---

## 🖥 Реализованные формы

### Главная форма – список заказов (`OderListForm`)

- Отображение таблицы заказов с полями: ID, клиент, дата, сумма, статус, адрес доставки.
- Кнопки: **Создать**, **Редактировать**, **Удалить**, **Обновить**.
- Двойной клик по строке открывает форму редактирования.

### Форма редактирования/создания заказа (`OderFormUnit`)

- Выбор клиента через диалог (`OderSelectionDialog`).
- Автоматическое заполнение полей: имя, телефон, адрес клиента.
- Возможность добавлять/удалять товары в заказе (через `DataGridView`).
- Выбор статуса заказа («Доставлено» / «Отменен»).
- Дата заказа по умолчанию – текущая.
- Сохранение изменений в БД.

### Диалог выбора клиента (`OderSelectionDialog`)

- Выпадающий список всех клиентов.
- Возвращает выбранного клиента в основную форму.

---

## 📦 Инициализация базы данных тестовыми данными

В проекте предусмотрен метод `SeedDatabase`, который заполняет таблицы демонстрационными данными:

- 5 клиентов
- 6 способов оплаты
- 4 категории сотрудников
- 6 уровней квалификации
- 6 товаров
- 6 ингредиентов
- 5 сотрудников
- Связи товаров и ингредиентов (состав блюд)
- 5 заказов со связями к клиентам, платежам, сотрудникам и товарам

Для заполнения вызовите метод после создания контекста:

```csharp
using (var db = new ApplicationDbContext())
{
    SeedDatabase(db);
}
```

> ⚠️ Рекомендуется использовать в разработке, не в production.

---

## 🔄 Миграции Entity Framework Core

Проект использует миграции для синхронизации модели и базы данных.

### Создание новой миграции
```bash
dotnet ef migrations add NameOfMigration
```

### Применение миграций к БД
```bash
dotnet ef database update
```

### Генерация идемпотентного SQL-скрипта
```bash
dotnet ef migrations script --idempotent
```

---

## 🎯 Функциональные возможности

| Функция | Описание |
|:---|:---|
| 📋 **Просмотр заказов** | Таблица со всеми заказами, фильтрация по статусу |
| ➕ **Создание заказа** | Выбор клиента, добавление товаров, указание статуса и оплаты |
| ✏️ **Редактирование заказа** | Изменение состава товаров, статуса, данных клиента |
| 🗑 **Удаление заказа** | Удаление записи заказа из БД |
| 👥 **Управление клиентами** | Выбор из существующих (расширение возможно) |
| 📦 **Состав товаров** | Каждый товар состоит из ингредиентов с количеством |
| 🧑‍🍳 **Назначение сотрудника** | К каждому заказу привязан ответственный сотрудник |

---

## 📝 Примечания

- Проект разработан в рамках учебной курсовой работы.
- Для корректной работы необходимо наличие установленного **Microsoft Access Database Engine** (или Microsoft Office).
- Файл базы данных `dodoDb2.accdb` создаётся автоматически в рабочей папке приложения.
- При первом запуске БД будет пустой. Для наполнения тестовыми данными используйте `SeedDatabase`.

---

## 📄 Лицензия

Проект создан в учебных целях. Все права принадлежат автору.
```

